// https://cses.fi/problemset/task/1160

#include <bits/stdc++.h>

using namespace std;

#define lli long long

void solve()
{
    int n, q;
    cin >> n >> q;
    int next[n + 1];
    for (int i = 1; i < n + 1; i++)
    {
        cin >> next[i];
    }
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();

    return 0;
}